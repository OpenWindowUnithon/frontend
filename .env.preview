# Loaded by `vite build --mode preview` (see `deploy:preview`), not by plain
# `bun run build` -- lets the app tell preview and production deployments
# apart (see shared/config/env.ts's VITE_APP_ENV).
VITE_API_BASE_URL=https://naru-backend.perasite.dev
VITE_APP_ENV=preview
