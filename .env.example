# Spring backend (see https://github.com/OpenWindowUnithon/backend).
#
# Leave empty for local dev — vite.config.ts's /api proxy forwards to
# localhost:8080, sidestepping the backend's currently-missing CORS config.
# Set to a real URL once CORS is fixed backend-side, for deployed use.
VITE_API_BASE_URL=
